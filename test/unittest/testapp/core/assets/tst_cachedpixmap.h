#ifndef TESTCACHEDPIXMAP_H
#define TESTCACHEDPIXMAP_H

#include <QObject>

class TestCachedPixmap : public QObject
{
    Q_OBJECT

public:
    explicit TestCachedPixmap(QObject *parent = 0);

private Q_SLOTS:
    
};

#endif // TESTCACHEDPIXMAP_H